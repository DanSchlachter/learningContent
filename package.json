{
  "name": "learning-content",
  "version": "1.0.0",
  "description": "Learning Content Management — curate and publish links to external learning resources",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4"
  },
  "devDependencies": {
    "@sap/cds-dk": "^8",
    "@cap-js/sqlite": "^1"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build --production"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite"
      },
      "auth": {
        "[development]": {
          "kind": "mocked",
          "users": {
            "alice": {
              "password": "alice",
              "roles": ["Admin"]
            },
            "bob": {
              "password": "bob",
              "roles": ["Admin"]
            },
            "carol": {
              "password": "carol",
              "roles": []
            }
          }
        }
      }
    }
  }
}
